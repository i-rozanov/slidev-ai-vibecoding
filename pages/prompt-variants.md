---
transition: slide-up
---

# Вариант 1: Анатомия идеального промпта

## Структура эффективного запроса

```python
# Идеальный промпт содержит:
prompt = {
    "context": "Предыдущие решения и контекст",
    "task": "Конкретная, измеримая задача",
    "constraints": "Ограничения и требования",
    "examples": "Примеры желаемого результата",
    "format": "Требуемый формат вывода"
}
```

**Ключевые элементы:**
- 🎯 **Конкретность** - четкая постановка задачи
- 📝 **Примеры** - демонстрация ожидаемого формата
- ⚖️ **Ограничения** - явное указание границ
- 🔄 **Итеративность** - возможность уточнения

<!--
Хороший промпт - это искусство баланса между детализацией и гибкостью.
-->

---
transition: slide-up
---

# Вариант 2: Уровни детализации промпта

## От общего к частному

**Уровень 1: Общая задача**
```
"Напиши функцию для работы с датами"
```

**Уровень 2: Конкретизация**
```
"Создай функцию на Python, которая принимает дату в формате YYYY-MM-DD 
и возвращает разницу в днях от текущей даты"
```

**Уровень 3: Детальные требования**
```
"Напиши на Python функцию days_since(date_str), которая:
- Принимает строку в формате 'YYYY-MM-DD'
- Возвращает целое число дней разницы
- Использует модуль datetime
- Обрабатывает некорректный формат исключением ValueError
- Включает doctest примеры"
```

**Уровень 4: Стиль и ограничения**
```
"Используй type hints, пиши по PEP8, добавь docstring, 
обработай крайние случаи, протестируй на примерах:
- '2024-01-01' (прошлое)
- Сегодняшняя дата
- '2025-12-31' (будущее)"
```

<!--
Постепенное уточнение - ключ к получению качественных результатов от ИИ.
-->

---
transition: slide-up
---

# Вариант 3: Практические примеры промптов

## Плохой vs Хороший промпт

**❌ Плохой промпт:**
```
"Сделай валидацию email"
```

**✅ Хороший промпт:**
```
"Напиши на Python функцию validate_email(email), которая:
- Проверяет корректность формата email с помощью regex
- Проверяет существование домена через DNS lookup
- Возвращает tuple (is_valid: bool, error_message: str)
- Обрабатывает edge cases: None, пустая строка, спецсимволы
- Напиши unit tests с pytest
- Используй только стандартные библиотеки Python"
```

**🎯 Отличный промпт:**
```
"Как senior Python developer, создай production-ready функцию 
валидации email. Требования:
- Вход: строка email
- Выход: (bool, str) с результатом и ошибкой
- Проверка формата RFC 5322
- DNS проверка MX записей
- Кэширование DNS запросов на 5 минут
- Thread-safe implementation
- 100% coverage pytest tests
- Type hints и docstring
- Обработай все возможные исключения
Верни полный код с импортами и тестами."
```

<!--
Качество результата напрямую зависит от качества промпта - чем конкретнее, тем лучше.
-->
